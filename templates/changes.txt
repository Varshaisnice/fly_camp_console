<!-- Page 5: Initialisation -->
<div class="screen" id="page_initializing">
  <div class="content-box init-wrap">

    <!-- RETRY ERROR PANEL – ON TOP, CENTERED, SMALL -->
    <div id="init-error" class="init-error hidden">
      <div class="error-content">
        <p id="init-error-text">Initialisation failed. Please retry.</p>
        <div class="init-actions">
          <button class="btn-cta small" id="btn-init-retry" onclick="retryConnectionCheck()">Retry</button>
          <button class="btn-ghost small" id="btn-init-back" onclick="backToChoose()">Back</button>
        </div>
      </div>
    </div>

    <!-- FULL INSTRUCTIONS (Hidden on small screens) -->
    <div class="full-instructions">
      <!-- Generic Flying Instructions -->
      <div class="generic-rules">
        <h3 class="rules-title">Generic Flying Instructions</h3>
        <p class="muted" style="margin-bottom:16px;">After reading the rules, click “Start Game.”</p>

        <div class="point-cards">
          <div class="point-card"><span class="point-bullet"></span><p>Place your hand over the <strong>mirror surface</strong> for <strong>3 seconds</strong> to launch the drone.</p></div>
          <div class="point-card"><span class="point-bullet"></span><p>The <strong>120-second timer</strong> begins only when you score your <strong>first point</strong>.</p></div>
          <div class="point-card"><span class="point-bullet"></span><p>Fly <strong>smoothly</strong>, stay inside the arena, and aim for <strong>accuracy</strong>.</p></div>
          <div class="point-card"><span class="point-bullet"></span><p>Score as many points as possible before time runs out.</p></div>
          <div class="point-card"><span class="point-bullet"></span><p><strong>Most important rule: Have fun!</strong></p></div>
        </div>
      </div>

      <!-- Dynamic game rules -->
      <div id="game-rules-dynamic" class="game-rules">
        <div id="rules-dynamic" class="rule-set hidden">
          <h3 id="rules-title-dynamic" class="rules-title"></h3>
          <div class="point-cards" id="rules-list-dynamic"></div>
        </div>
      </div>

      <!-- Initialization section -->
      <div class="spinner" aria-hidden="true"></div>
      <h2>Initialising game components</h2>
      <p id="init-status" class="muted">Running connection checks...</p>
    </div>

    <!-- DEVICE STATUS – ALWAYS VISIBLE -->
    <div class="device-status">
      <div class="device-counter">
        <span id="devices-ready">0</span> / <span id="devices-total">4</span> devices initialized
      </div>

      <div id="init-steps" class="steps">
        <!-- JS fills this -->
      </div>

      <div class="btn-row big mt-8">
        <button class="btn-cta" id="btn-start-game" disabled>Start Game</button>
        <button class="btn-ghost" onclick="backToChoose()">Back</button>
      </div>
    </div>

  </div>
</div>











/* -------------------------------------------------
   MOBILE-OPTIMIZED INITIALIZATION
   ------------------------------------------------- */

/* ERROR PANEL – ON TOP, CENTERED, SMALL */
.init-error {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  z-index: 5000;
  width: min(340px, 86vw);
  max-width: 380px;
  padding: 18px 22px;
  border-radius: 16px;
  background: linear-gradient(135deg, rgba(255, 82, 82, 0.28), rgba(255, 50, 50, 0.2));
  border: 2px solid #ff5252;
  box-shadow: 0 10px 28px rgba(0, 0, 0, 0.45), 0 0 22px rgba(255, 82, 82, 0.6);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);
  text-align: center;
  animation: popIn 0.4s ease;
}

.error-content { gap: 14px; }
#init-error-text { font-size: clamp(15.5px, 2.1vw, 18px); font-weight: 600; color: #ffdddd; }

.init-actions {
  display: flex;
  gap: 10px;
  justify-content: center;
  flex-wrap: wrap;
}

.init-actions .btn-cta.small,
.init-actions .btn-ghost.small {
  padding: clamp(9px, 1.5vw, 12px) clamp(16px, 2.6vw, 22px);
  font-size: clamp(14.5px, 1.7vw, 17px);
  min-width: 90px;
  border-radius: 12px;
}

/* FULL INSTRUCTIONS – HIDDEN ON SMALL SCREENS */
.full-instructions {
  display: block;
}

/* DEVICE STATUS – ALWAYS VISIBLE */
.device-status {
  width: 100%;
  max-width: 700px;
  margin: 0 auto;
}

.device-counter {
  font-size: clamp(19px, 2.4vw, 26px);
  font-weight: 700;
  color: var(--accent);
  margin: 16px 0 14px;
  text-shadow: 0 0 12px rgba(191, 0, 255, 0.7);
  text-align: center;
}

#devices-ready {
  background: linear-gradient(135deg, #00ff88, #00b86e);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  font-size: 1.35em;
}

/* CHECKMARK STEPS */
.steps {
  display: flex;
  flex-direction: column;
  gap: 12px;
  margin: 0 auto;
  width: 100%;
}

.step {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 14px 18px;
  background: rgba(191, 0, 255, 0.15);
  border: 1.5px solid rgba(191, 0, 255, 0.38);
  border-radius: 16px;
  font-size: clamp(15px, 1.85vw, 18.5px);
  font-weight: 500;
  color: rgba(255, 255, 255, 0.95);
  opacity: 0;
  transform: translateX(-20px);
  animation: slideIn 0.5s forwards;
  backdrop-filter: blur(9px);
}

.step .checkbox {
  width: 28px;
  height: 28px;
  border: 2.5px solid rgba(191, 0, 255, 0.6);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.step .checkbox::before {
  content: '✓';
  font-size: 17px;
  font-weight: 900;
  color: #00ff88;
  opacity: 0;
  transform: scale(0);
  transition: all 0.3s ease;
}

.step.ok .checkbox {
  background: rgba(0, 255, 136, 0.28);
  border-color: #00ff88;
  box-shadow: 0 0 14px rgba(0, 255, 136, 0.6);
}

.step.ok .checkbox::before {
  opacity: 1;
  transform: scale(1);
  animation: checkPop 0.35s ease;
}

.step.fail .checkbox::before {
  content: '✗';
  color: #ff5252;
  opacity: 1;
  transform: scale(1);
}

.step.fail {
  background: rgba(255, 87, 87, 0.18);
  border-color: #ff6b6b;
  color: #ffaaaa;
}

/* START BUTTON */
#btn-start-game:not(:disabled) {
  background: linear-gradient(135deg, #00ff88, #00b86e);
  color: #000;
  box-shadow: 0 0 30px rgba(0, 255, 136, 0.7);
  animation: pulseReady 2s infinite;
}

#btn-start-game:not(:disabled):hover {
  transform: translateY(-3px) scale(1.02);
  box-shadow: 0 0 40px rgba(0, 255, 136, 0.9);
}

/* RESPONSIVE: Hide full instructions on small screens */
@media (max-width: 768px) {
  .full-instructions {
    display: none !important;
  }
  .device-status {
    margin-top: 20px;
  }
  .device-counter {
    font-size: clamp(20px, 3vw, 28px);
    margin: 8px 0 16px;
  }
  .steps {
    gap: 10px;
  }
  .step {
    padding: 12px 16px;
    font-size: clamp(14.5px, 2vw, 17px);
  }
}

/* Animations */
@keyframes popIn {
  0% { opacity: 0; transform: translate(-50%, -50%) scale(0.85); }
  70% { transform: translate(-50%, -50%) scale(1.06); }
  100% { opacity: 1; transform: translate(-50%, -50%) scale(1); }
}
@keyframes slideIn { to { opacity: 1; transform: translateX(0); } }
@keyframes checkPop { 0% { transform: scale(0); } 70% { transform: scale(1.3); } 100% { transform: scale(1); } }
@keyframes pulseReady { 0%,100% { box-shadow: 0 0 30px rgba(0,255,136,.7); } 50% { box-shadow: 0 0 40px rgba(0,255,136,1); } }
